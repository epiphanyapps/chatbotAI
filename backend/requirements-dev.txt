# Development / test-only dependencies.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
aiosqlite>=0.19.0  # async SQLite driver used by the test suite (in-memory DB)
